# Guess the AI — Backend

**All product, 0G stack, contracts, DA, storage, compute, API reference, and environment variables are documented in the repository root:**

### → [`README.md`](../README.md)

Open that file at the top level of this repo (it is also what GitHub shows on the main repository page).

---

### Run this package locally

```bash
npm i
cp .env.example .env
mkdir -p ./cache/orig
npm run dev
```

Requires Node 20+, MongoDB, and Redis. Configure `.env` using variables described in the root README and `.env.example`.
